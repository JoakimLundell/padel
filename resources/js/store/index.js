import Vue from 'vue'
import Vuex from 'vuex'

import auth from './auth'

Vue.use(Vuex)

const state = {
    
}

const mutations = {
    
}

const getters = {
    
}

const actions = {
    
}

const store = new Vuex.Store({
    state,
    mutations,
    actions,
    getters,
    modules: {
        auth
    }
})

export default store